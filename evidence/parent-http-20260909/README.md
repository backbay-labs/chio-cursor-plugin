# Parent-owned HTTP discovery candidate

The trusted parent owns the kernel credential, Chio gateway and durable journal. The guest receives only a route-scoped ephemeral token for its single MCP server. The default-deny SBPL permits that route while denying direct kernel access and operator/journal access.

The packaged installation used an empty npm cache, offline mode and no install scripts. The actual pinned Cursor CLI approved the server and discovered exactly four tools. The retained exact profile was then tested with a Node process and descendant: operator reads, journal reads/writes and control writes were denied; isolated state writes succeeded; a positive listener bound to the permitted parent port recorded two connections; direct kernel connections failed.

The readonly dispatch observation was captured on the first cold package run. The r2 archive changes documentation and retains identical launcher/boundary code; r2 independently repeated discovery and actual-profile probes. No model-driven protected effect or authenticated provider request was attempted. The unchanged prompt refusal and missing authenticated hosted-protocol qualification remain acceptance blockers. All I01-I08 gates remain open.

Earlier worker boundary files and protocol inspection are preserved under evidence/final. They describe their exact historical candidates. The current operator procedures describe the new parent transport.
